defmodule Klaus.Runner do
  # Check the module is loaded
  # Read the input file
  # Start clock
  # Call part1
  # Stop clock
  # Do the same for optional part 2
  @spec run_day(integer()) :: nil
  def run_day(day) do
  end

  # Regex for all modules that exist
  # For each one found, invoke run_day
  @spec run_all() :: nil
  def run_all() do
  end
end
